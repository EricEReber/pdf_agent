# An AI agent that extracts fields from handwritten documents
Built using Anthropic's Claude AI agents for a test case at Bayer Norge. The agent can extract the fields from any PDF document and convert them into structured tabular data (a .db file).
